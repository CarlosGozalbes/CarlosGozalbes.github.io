name: My Workflow
on: [push]

jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Install Dependencies
        run: npm install
      
      - name: Send Email
        env:
          EMAILJS_API: ${{ secrets.EMAILJS_API }}
          EMAILJS_SERVICE: ${{ secrets.EMAILJS_SERVICE }}
          EMAILJS_TEMPLATE: ${{ secrets.EMAILJS_TEMPLATE }}
        run: |

